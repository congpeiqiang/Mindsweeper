# 查询逻辑

提问问题

->检索相关表结构， 返回

```
{
    "tables": tables_list,
    "columns": columns_list,
    "relationships": relationships_list
}
```

​	使用LLM分析查询并提取关键实体和意图

​	连接neo4j，获取所有表名及其描述

​	查找与查询相关的表，基于上述实体、意图和所有表名和描述查找相关表 ->  相关系数大于n的表名或表id relevant_tables_dict

​    查找与查询相关的列,  返回列名或列描述包含实体，以及包含列名的表，并合并至上述表 relevant_tables_dict

​	查找relevant_tables_dict中表的一跳外键表

​    使用LLM评估扩展表是否真正与查询相关

​	按相关性分数排序表sorted_tables

​	若sorted_tables为空，则获取所有表

​	获取sorted_tables表的所有字段

​	获取表、字段的关系

​	

->获取值映射

->构建提示

->调用LLM API

->从响应中提取SQL

->使用值映射处理SQL

->验证SQL

->执行SQL